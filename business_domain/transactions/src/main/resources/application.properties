server.port=8082
spring.application.name=business-domain-transactions
eureka.client.serviceUrl.defaultZone=${EUREKA_SEVER:http://localhost:8761/eureka}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Admin Server Properties
#management.endpoints.web.exposure.include=* -> As with Spring Boot 2 most of the endpoints aren’t exposed via http by default, we expose all of them. For production you should carefully choose which endpoints to expose
management.endpoints.web.exposure.include=*
spring.profiles.active=@activatedProperties@
# if you'd like to expose shutdown:
management.endpoint.shutdown.enabled=true