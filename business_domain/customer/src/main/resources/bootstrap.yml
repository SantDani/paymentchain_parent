
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      failFast: true
      #name of base properties config file, before profile
      name: config-client
      profile: dev
      #label: latest
      username: admin
      password: qwerty
      retry: 
        max-attempts: 1
        max-interval: 1000